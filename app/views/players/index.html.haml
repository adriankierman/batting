- model_class = Player
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize
%table#players-table.table.table-striped{"data-source"=>(players_path(format: :json)+"?datatables=1")}
  %thead
    %tr
      %th= model_class.human_attribute_name(:given_name)
      %th= model_class.human_attribute_name(:surname)
      %th= model_class.human_attribute_name(:runs)
      %th= model_class.human_attribute_name(:home_runs)
      %th= model_class.human_attribute_name(:steals)
      %th= model_class.human_attribute_name(:rbi)
      %th= "Avg"
      %th= "Ops"
      %th= model_class.human_attribute_name(:position)
      -#%th=t '.actions', :default => t("helpers.actions")
  %tbody
    -#- @players.each do |player|
    -#  %tr
    -#    %td= player.given_name
    -#    %td= player.surname
    -#    %td= player.position
    -#    %td= player.runs
    -#    %td= player.home_runs
    -#    %td= player.steals
    -#    %td= player.rbi
    -#    %td
    -#      = link_to t('.edit', :default => t("helpers.links.edit")), edit_player_path(player), :class => 'btn btn-mini'
    -#      = link_to t('.destroy', :default => t("helpers.links.destroy")), player_path(player), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

-#= link_to t('.new', :default => t("helpers.links.new")), new_player_path, :class => 'btn btn-primary'
